import 'zone.js/dist/zone';  // Included with Angular CLI.

if (typeof window !== 'undefined' && window['__Zone_disable_cross_context_check'] !== true) {
  window['__Zone_disable_cross_context_check'] = true;
}